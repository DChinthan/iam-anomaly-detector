scikit-learn>=1.4.0
pandas>=2.1.0
numpy>=1.26.0
tensorflow>=2.15.0
streamlit>=1.32.0
plotly>=5.19.0
boto3>=1.34.0
anthropic>=0.25.0
sqlalchemy>=2.0.0
scipy>=1.11.0
psycopg2-binary>=2.9.0   # only needed when DATABASE_URL points at Postgres/RDS or Cloud SQL
confluent-kafka>=2.3.0   # only needed when STREAM_MODE=kafka
google-cloud-logging>=3.8.0    # only needed when CLOUD_PROVIDER=gcp with GCP_MOCK=false
google-cloud-firestore>=2.14.0 # only needed when CLOUD_PROVIDER=gcp with GCP_MOCK=false
google-cloud-pubsub>=2.19.0    # only needed when STREAM_MODE=pubsub
